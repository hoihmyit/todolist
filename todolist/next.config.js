/** @type {import('next').NextConfig} */
const nextConfig = {}
// next.config.js
module.exports = {
  reactServer: {
    version: 1, // Phiên bản React Server Components
  },
};

module.exports = nextConfig
